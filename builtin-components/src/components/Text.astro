---
const { bg, fg } = Astro.props;
---

<div class="text">
  <slot />
</div>

<style define:vars={{ bg, fg }}>
  .text {
    background: var(--bg, yellow);
    color: var(--fg, black);
    display: inline-block;
    padding: 0.5rem;
  }
</style>
